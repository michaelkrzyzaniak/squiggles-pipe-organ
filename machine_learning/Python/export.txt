#! /bin/bash

source venv/bin/activate

python main.py \
     --sessions_root=lstm_sessions \
     --session_folder=lstm_test  \
     --data_folder=required_but_ignored \
     --use_cpu=True \
     --export=True
deactivate
