#! /bin/bash

source venv/bin/activate

python train.py \
     --data_root=data \
     --sessions_root=lstm_sessions \
     --data_folder=bach_multiinstrumental \
     --session_folder=lstm_test_1 \
     --num_batches=1000 \
     --save_every=100 \
     --use_cpu=True
deactivate
